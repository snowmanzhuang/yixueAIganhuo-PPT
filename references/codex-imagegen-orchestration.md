# Deprecated Codex Image Generation Orchestration

This file is retained only to mark the old orchestration path as inactive.

Current yixueAIganhuo-PPT slide image generation must use:

```bash
editppt image generate/edit/batch
```

Do not use built-in non-`editppt image` generation tools for the current workflow. The active backend contract is documented in `references/provider-config.md` and the parent orchestration is documented in `references/medical-integrated-workflow.md`.
